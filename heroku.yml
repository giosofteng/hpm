build:
    - docker:
        worker-collector: ./collector/Dockerfile
    - docker:
        worker-transformer: ./transformer/Dockerfile
    - docker:
        web: ./renderer/Dockerfile
        worker-renderer: ./renderer/Dockerfile
